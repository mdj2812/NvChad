local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "cpp", "python" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
